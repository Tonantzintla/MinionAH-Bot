{
  "name": "minionah-bot",
  "version": "0.0.1",
  "dependencies": {
    "axios": "^1.7.7",
    "discord.js": "^14.16.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4",
    "typescript": "^5.5.4"
  },
  "scripts": {
    "build-run": "pnpm exec tsc && node ./dist",
    "dev": "pnpm exec nodemon -e ts --watch 'src/**/*.ts' --exec 'pnpm run build-run'"
  }
}
